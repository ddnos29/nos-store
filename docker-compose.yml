version: '3.8'
services:
   mongodb:
      image: mongo
      ports:
         - '27017:27017'
      volumes:
         - /data:/data/db
      environment:
         MONGO_INITDB_ROOT_USERNAME: root
         MONGO_INITDB_ROOT_PASSWORD: root

   server:
      build: ./server
      container_name: nos-store-server
      volumes:
         - ./server:/app
         - /app/node_modules
      ports:
         - '3000:3000'
      env_file:
         - ./server/.env
      depends_on:
         - mongodb
